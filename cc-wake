#!/usr/bin/env bash
# Convenience launcher. Runs the module from the repo without installing.
#   ./cc-wake                  read-only dashboard on http://127.0.0.1:8787
#   ./cc-wake --enable-control focus + enable RC (loopback only)
cd "$(dirname "$0")" && exec python3 -m cc_wake "$@"
